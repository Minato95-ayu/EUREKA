apiVersion: v1
kind: Service
metadata:
  name: eureka-frontend
  namespace: eureka
spec:
  selector:
    app: eureka-frontend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 80
  type: ClusterIP
