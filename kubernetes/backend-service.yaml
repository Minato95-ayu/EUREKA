apiVersion: v1
kind: Service
metadata:
  name: eureka-backend
  namespace: eureka
spec:
  selector:
    app: eureka-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
